# Valid minimal agent template for testing
metadata:
  name: test-agent
  version: 1.0.0
  description: A test agent for unit testing

agent:
  description: Test agent for validating loader
  prompt: |
    You are a test agent.
    Perform test operations.
  tools:
    - Read
    - Write
