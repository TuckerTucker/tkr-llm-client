# Multi-document YAML with multiple tool configs
---
tool:
  name: Read
  permissions:
    allowedPaths: ["**/*.ts"]
---
tool:
  name: Write
  permissions:
    requireConfirmation: true
---
tool:
  name: Edit
  permissions:
    allowedPaths: ["src/**"]
