metadata:
  name: test-template
  version: 1.0.0
  description: Simple test template

agent:
  description: Test agent
  prompt: |
    Test prompt with {{ var1 }} and {{ var2 | default: default-value }}.
  tools:
    - Read

validation:
  required:
    - var1

runtime:
  workingDirectory: "{{ cwd }}"
